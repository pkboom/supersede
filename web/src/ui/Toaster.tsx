/**
 * Phase 4 stub. Phase 7 fleshes this out with `useToaster()` Context,
 * 5s auto-dismiss, manual dismiss, and the conflict/rate-limit/save-error
 * channels. Until then this renders nothing — toasts wired to it in
 * Phase 7 are no-ops, which is the safest default for a single-user app.
 */
export function Toaster() {
  return null;
}
