{
  "name": "email-design-system",
  "version": "0.0.1",
  "private": true,
  "description": "Local single-user MJML email designer.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup": "node src/cli/setup.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "components": "tsx src/cli/component-slice.ts",
    "measure": "tsx src/cli/measure-templates.ts",
    "handover": "tsx src/cli/handover.ts"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.6",
    "mjml": "^4.18.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "fast-check": "^3",
    "tsx": "^4",
    "typescript": "^5.4",
    "vitest": "^1"
  }
}
