{
  "name": "email-design-system",
  "version": "0.0.1",
  "private": true,
  "description": "Local single-user HTML email component extractor.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "handover": "node src/handover.js",
    "run": "node src/index.js"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.6",
    "parse5": "^8.0.1"
  },
  "devDependencies": {
    "fast-check": "^3",
    "vitest": "^1"
  }
}
